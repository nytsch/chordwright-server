{
  "name": "chordwright-server",
  "version": "1.0.0",
  "description": "A folder of .chordpro files, served to the Chordwright app — with revisions, conflict detection and a Home Assistant add-on.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": { "node": ">=20" },
  "bin": { "chordwright-server": "chordwright-data/server/serve.mjs" },
  "scripts": {
    "start": "node chordwright-data/server/serve.mjs --dir ./data",
    "test": "node --test test/server.test.mjs"
  },
  "repository": { "type": "git", "url": "https://github.com/nytsch/chordwright-server.git" }
}
